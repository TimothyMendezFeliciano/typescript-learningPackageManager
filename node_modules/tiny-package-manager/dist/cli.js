#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const yargs = require("yargs");
const _1 = require(".");
// This file is for CLI usage.
// There isn't too much logic about package manager here.
// For details please consult the documentation of `yargs` module.
yargs
    .usage('tiny-pm <command> [args]')
    .version()
    .alias('v', 'version')
    .help()
    .alias('h', 'help')
    .command('install', 'Install the dependencies.', argv => argv.option('production', {
    type: 'boolean',
    description: 'Install production dependencies only.'
}), argv => _1.default(argv.production))
    .command('*', 'Install the dependencies.', argv => argv.option('production', {
    type: 'boolean',
    description: 'Install production dependencies only.'
}), argv => _1.default(argv.production))
    .parse();
